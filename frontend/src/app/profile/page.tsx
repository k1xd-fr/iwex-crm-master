import Profiles from "@/components/Profile";

const Profile: React.FC = () => {
  return (
    <div
      style={{
        background: "#fc0",
        padding: "2% 0",
        height: "100%",
      }}
    >
      <Profiles></Profiles>
    </div>
  );
};

export default Profile;
